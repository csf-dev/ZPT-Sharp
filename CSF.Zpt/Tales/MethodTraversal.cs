using System;

namespace CSF.Zpt.Tales
{
  /// <summary>
  /// Delegate represents a traversal using a method instance discovered via reflection.
  /// </summary>
  public delegate object MethodTraversal(object source);
}

